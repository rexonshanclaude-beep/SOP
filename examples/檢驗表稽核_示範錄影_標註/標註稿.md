# 錄影標註稿

| 項目 | 內容 |
|------|------|
| 來源影片 | `檢驗表稽核_示範錄影.mp4` |
| 影片長度 | 02:36（156.0 秒）· 1280×720 |
| 標註點 | 3 個 |
| 順序 | 與影片時間先後一致 |
| 匯出時間 | 2026-08-12 21:55 |

> **給 AI 的說明**
> 1. 這是使用者親手標的操作流程，每個標註點大致對應 SOP 的一個步驟。
> 2. 下方的編號順序就是**正確的流程順序**，請照這個順序建步驟。
> 3.「這一步耗時」是這個標註點到時間軸上下一個標註點的秒數，可當作該步驟「每件時間」的候選值（來源標記為：錄影實測）。
> 4. 截圖在 `shots/`，**請實際讀圖**確認系統名稱、檔案名稱與畫面內容，不要只看註解。
> 5. 截圖上的標記：**箭頭／手指／游標／框選**是使用者指出「這裡才是重點」，請優先看那個位置；**編號**①②③是同一步之內的操作順序；**遮蔽**的黑塊是刻意蓋掉的敏感資料，不必追問內容；**放大擷取**的圖只留了畫面的一部分，看不到整個視窗是正常的。
> 6. 錄影只錄得到「做一件的樣子」。**一天做幾件、多久做一次、幾個人做，錄影裡看不出來，一定要另外問使用者。**

## 一覽

| # | 時間 | 這一步耗時 | 標記 | 註解摘要 |
|---|------|-----------|------|---------|
| 1 | 00:12.0 | 46.0 秒 | 箭頭、框選 | 在品質管理系統開啟今天的異常檢驗表清單，只挑「待處理」的那幾筆。清單每天 |
| 2 | 00:58.0 | 73.0 秒 | 箭頭、遮蔽 | 到 ERP 查這家供應商的聯絡窗口。窗口電話屬於個資，示範時用遮蔽蓋掉。 |
| 3 | 02:11.0 | 25.0 秒 | 箭頭、框選 | 寫退件通知信。主旨一定要帶單號，不然對方回信時對不起來。 |

---

## 1. 在品質管理系統開啟今天的異常檢驗表清單，只挑「待處理」的那幾筆。清單每天早上會更新。

- 影片時間：**00:12.0**
- 這一步耗時：**46.0 秒**（到 00:58.0 的下一刀）
- 截圖：`shots/01_t000012.jpg`（使用者標記了：箭頭、框選）

在品質管理系統開啟今天的異常檢驗表清單，只挑「待處理」的那幾筆。清單每天早上會更新。

## 2. 到 ERP 查這家供應商的聯絡窗口。窗口電話屬於個資，示範時用遮蔽蓋掉。

- 影片時間：**00:58.0**
- 這一步耗時：**73.0 秒**（到 02:11.0 的下一刀）
- 截圖：`shots/02_t000058.jpg`（使用者標記了：箭頭、遮蔽）

到 ERP 查這家供應商的聯絡窗口。窗口電話屬於個資，示範時用遮蔽蓋掉。

## 3. 寫退件通知信。主旨一定要帶單號，不然對方回信時對不起來。

- 影片時間：**02:11.0**
- 這一步耗時：**25.0 秒**（到影片結束）
- 截圖：`shots/03_t000131.jpg`（使用者標記了：箭頭、框選）

寫退件通知信。主旨一定要帶單號，不然對方回信時對不起來。

---

## 接下來

把這個資料夾交給 Claude，說「幫我把這份標註做成 SOP」。Claude 會：

1. 讀完這份與 `shots/` 的截圖，整理出現況流程草稿
2. 跟你確認每一步的件數、頻率、人數（錄影裡沒有這些）
3. 一起規劃 AI 導入後的流程（含維護與審核，不然效益會高估）
4. 產出交件檔 `.sop.html`，並問你要不要上傳給 AI 發展部
